# OneMix 用户使用手册

本文档面向业务同学与运营同学，帮助你从 0 到 1 完成商品主图、详情图与短视频生成，并一键打包全部素材。

## 目录

- [适用对象与目标](#适用对象与目标)
- [使用前准备](#使用前准备)
- [启动方式（本机）](#启动方式本机)
- [页面总流程](#页面总流程)
- [步骤一：提取商品信息](#步骤一提取商品信息)
- [步骤二：生成主图](#步骤二生成主图)
- [步骤三：生成详情图](#步骤三生成详情图)
- [步骤四：生成视频](#步骤四生成视频)
- [Key 配置说明](#key-配置说明)
- [下载与结果管理](#下载与结果管理)
- [常见问题 FAQ](#常见问题-faq)
- [最佳实践](#最佳实践)

## 适用对象与目标

OneMix 适合以下场景：

- 电商上新：快速产出主图、详情图和短视频
- 老品翻新：在已有素材基础上迭代风格
- 批量运营：多槽位并行生成，统一下载归档

你可以完成：

- 上传商品资料并提取结构化关键信息
- 规划主图 / 详情图 / 视频提示词并批量生成
- 单槽位重构提示词并单独再生图 / 生视频
- 复用已生成主图、详情图作为下一轮参考图
- 将主图、详情图、视频按分类打包下载

## 使用前准备

- 已安装 Python 3.10+、Node.js 18+、npm
- 网络可访问你使用的模型服务
- 已准备至少一张白底参考图（建议清晰、主体完整）
- 已准备模型 Key：
  - 阿里百炼（DashScope）：OCR、信息提取、千问生图等
  - 火山方舟（ARK）：Seedream 生图、Seedance 生视频（需在控制台单独开通模型）

## 启动方式（本机）

### 1) 启动后端（默认端口 `8767`）

Windows:

```powershell
cd OneMix
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python backend\run_server.py
```

macOS / Linux:

```bash
cd OneMix
source .venv/bin/activate
pip install -r requirements.txt
python backend/run_server.py
```

### 2) 启动前端（默认端口 `5173`）

```powershell
cd frontend
npm install
npm run dev
```

浏览器打开终端提示地址（通常为 `http://127.0.0.1:5173`）。

Docker 部署请参考仓库根目录 `DOCKER.md`。

## 页面总流程

建议按进度条顺序操作：

1. 步骤一：信息提取
2. 步骤二：生成主图
3. 步骤三：生成详情
4. 步骤四：生成视频

说明：

- 「下一步」需在当前步骤有生成结果后才可点击（避免空结果进入下一步）
- 右上角「运行日志」可查看失败原因与执行状态
- 右上角「API Key 设置」用于保存 Key，并可跳转官方控制台

## 步骤一：提取商品信息

### 1) 上传输入资料

- 可上传文本文件（如 `docx` / `txt` 等）
- 可点击「OCR 识别」上传商品图片提取文字
- 也可直接编辑「合并文本（可编辑）」

### 2) 执行 OCR（可选）

- 打开 OCR 弹窗后，点击或拖拽上传图片（支持多选）
- 点击「OCR 识别（仅提取文字）」后会显示「识别中…」处理状态，期间请勿关闭弹窗
- 识别完成后可编辑结果，再「合并到描述文本」

> 若弹窗提示需配置 API Key：请到右上角「API Key 设置」中添加，并自行开通相关服务。

### 3) 提取关键信息

- 点击「提取关键信息」：生成结构化结果，并尽量自动回填商品名称等字段
- 若效果不理想，先手动修正文案再重新提取
- 商品卖点、规格参数、使用场景写得越完整，后续出图 / 出视频越稳定

## 步骤二：生成主图

### 1) 上传与管理参考图

- 支持多选上传白底商品图
- 每张图支持预览 / 删除 / 替换

### 2) 选择模型与数量

- 在表单中选择生图模型（豆包 Seedream / 千问 Qwen-Image 等）
- 设置主图数量
- 可按需「同步开通状态」，查看账号可见模型

### 3) 规划与生成

- 点击「生成主图提示词」
- 在表格中检查并调整：参考图、宽高比、分辨率、提示词
- 可用「重构」只改当前槽位提示词，「生图」只生成当前槽位
- 点击「一键生成主图」批量出图

### 4) 进入下一步

- 生成结果区可预览、单张下载、选为参考图、一键批量下载 ZIP
- 有主图结果后，点击「下一步：生成详情」

## 步骤三：生成详情图

操作方式与步骤二基本一致，差异如下：

### 1) 参考图来源

- 可继续上传白底图
- 也可点击「从主图库选择」：从「已生成主图」或「已上传原图」中选用

### 2) 规划与生成

- 点击「生成详情图提示词」→「一键生成详情图」
- 详情图默认更偏竖图比例（如 `9:16`，视当前模型能力而定）

### 3) 进入下一步

- 有详情图结果后，点击「下一步：生成视频」

## 步骤四：生成视频

### 1) 准备视频参考图（首帧）

支持三种方式：

- 本地上传 / 拖拽图片
- 点击「从图库选择」：
  - 已生成主图
  - 已生成详情图
  - 已上传参考图

建议优先选用主体清晰、构图稳定的主图或详情图作为首帧。

### 2) 选择视频模型与数量

- 默认模型：即梦 **Seedance 2.0 mini**
- 也可切换 Seedance 2.0 / fast（需账号已开通）
- 设置视频数量后，点击「生成视频提示词」

### 3) 生成视频

- 点击「一键生成视频」或表格内「生视频」
- 视频任务通常比生图更久，请耐心等待进度
- 结果区可预览（视频播放）、单条下载、批量 ZIP

### 4) 完成并一键打包

- 点击右侧「完成并一键打包」
- 系统会把已生成的主图、详情图、视频按分类打进同一个 ZIP，例如：

```text
商品名_全部素材.zip
├─ 主图/
│  ├─ main_01.jpg
│  └─ ...
├─ 详情/
│  ├─ detail_01.jpg
│  └─ ...
└─ 视频/
   ├─ video_01.mp4
   └─ ...
```

- 至少有一类生成结果时按钮才可点击

## Key 配置说明

点击右上角齿轮进入「API Key 设置」：

| 卡片 | 用途 | 控制台 |
| --- | --- | --- |
| 阿里百炼 (DashScope) | OCR、信息提取、千问生图等 | [打开阿里百炼控制台](https://bailian.console.aliyun.com/cn-beijing?tab=model#/api-key) |
| 火山方舟 | Seedream 生图、Seedance 生视频 | [打开火山方舟开通管理](https://console.volcengine.com/ark/region:cn-beijing/openManagement) |

建议：

- 使用 Seedream / Seedance 时必须配置火山方舟 Key，并在开通管理页开通对应模型
- 使用千问图生图时配置阿里百炼 Key
- Key 仅保存在本机 SQLite，请勿将数据库文件提交到公开仓库

## 下载与结果管理

| 方式 | 说明 |
| --- | --- |
| 单张 / 单条下载 | 结果卡片上的下载按钮 |
| 一键批量下载 | 当前步骤结果区 ZIP（仅含该任务产物） |
| 完成并一键打包 | 步骤四汇总主图 + 详情 + 视频，分类打包 |

建议按「日期-商品名-版本」命名归档；迭代时保留优质中间结果，便于回流为参考图。

## 常见问题（FAQ）

### Q1：点击生成后提示缺少 Key

请先在右上角「API Key 设置」保存对应服务 Key，并确认已开通相关模型，再重新发起任务。

### Q2：视频生成报 ModelNotOpen

表示火山方舟账号尚未开通 Seedance 模型。请到[开通管理](https://console.volcengine.com/ark/region:cn-beijing/openManagement)申请开通后重试；也可先切换到账号已开通的其他 Seedance 型号。

### Q3：任务中断或刷新后看不到进度

可重新发起任务；已导出到缓存目录的结果通常仍可下载或被一键打包收录。

### Q4：「下一步」按钮是灰色的

需要当前步骤已有生成结果（主图 / 详情图）后才会启用，避免空结果进入下一步。

### Q5：一键打包失败

请确认至少有一类结果；若仍失败，查看运行日志。常见原因包括服务刚重启导致任务索引丢失（需重新生成）等。

### Q6：参考图从哪里选？

- 详情步骤：主图库（已生成主图 / 已上传原图）
- 视频步骤：图库（已生成主图 / 已生成详情图 / 已上传参考图）

## 最佳实践

- 先完善步骤一信息，再做提示词规划
- 先批量生成，再对不满意槽位单独精修
- 把优质主图 / 详情图回流为下一轮参考图，逐轮提升稳定性
- 视频优先用主体清晰的详情图或主图作首帧，提示词写清运镜与卖点节奏
- 每轮结束用「完成并一键打包」归档，避免素材散落
